{
  "name": "books-search",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "cd server && node server.js",
    "client": "cd client && npm start",
    "server": "cd server && npm run server",
    "build": "npm install --prefix server && cd client && npm install && npm run build",
    "dev": "concurrently -n 'server,client' -c 'yellow,white' \"npm run server\"  \"npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
